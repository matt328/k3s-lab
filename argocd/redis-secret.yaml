apiVersion: v1
kind: Secret
metadata:
  name: argocd-redis
  namespace: argocd
  annotations:
    k3s-lab.dev/note: "Lab-only credential. Replace with SealedSecrets or ExternalSecrets for real environments."
type: Opaque
stringData:
  auth: k3s-lab-redis-password
