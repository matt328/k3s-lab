apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: observability-ingress-cluster-b
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "60"
spec:
  project: platform
  source:
    targetRevision: main
    path: gitops/infra/observability-ingress/cluster-b
  destination:
    name: cluster-b
    namespace: observability
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
