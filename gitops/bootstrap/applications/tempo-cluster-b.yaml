apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tempo-cluster-b
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "50"
spec:
  project: platform
  source:
    repoURL: https://github.com/matt328/k3s-lab.git
    targetRevision: main
    path: gitops/infra/tempo/cluster-b
  destination:
    name: cluster-b
    namespace: observability
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
