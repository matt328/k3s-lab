apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: artifact-registry

components:
  - ../../../components/lab-network

resources:
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
