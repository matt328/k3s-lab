apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: reposilite
spec:
  ingressClassName: traefik
  rules:
    - host: $(MAVEN_HOST)
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: reposilite
                port:
                  number: 80
