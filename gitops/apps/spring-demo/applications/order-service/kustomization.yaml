apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: spring-demo

components:
  - ../../../../components/lab-network

helmGlobals:
  chartHome: ../../../../../charts

helmCharts:
  - name: spring-boot
    releaseName: order-service
    namespace: spring-demo
    valuesFile: values.yaml
